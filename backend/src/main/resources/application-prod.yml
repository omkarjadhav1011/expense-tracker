spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/expensetracker_prod}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    com.omkar.expensetracker: INFO